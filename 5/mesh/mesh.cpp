#include "mesh.hpp"

Mesh::Mesh(std::string filename){
    // parsing each line
    // -- detect if line starts with 'v '
    // -- detect if line starts with 'vn'
    // -- detect if line starts with 'f'

}
